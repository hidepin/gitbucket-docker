version: '2'

services:
  app:
    build: gitbucket
    volumes:
      - /opt/docker/gitbucket/volumes/gitbucket/opt/gitbucket:/opt/gitbucket
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "8080:8080"
      - "29418:29418"
